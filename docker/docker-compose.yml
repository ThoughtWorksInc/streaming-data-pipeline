version: "3"
services:
    kafka:
        image: wurstmeister/kafka:1.1.0
        networks: internal

    zookeeper:
        image: zookeeper:3.3.6
        networks: internal

networks:
    internal:

