version: "3"
services:
    kafka:
        image: wurstmeister/kafka:1.1.0
        networks:
          - streaming-data-internal

    zookeeper:
        image: zookeeper:3.3.6
        networks:
          - streaming-data-internal

networks:
    streaming-data-internal:

